# 로컬의 node_modules는 복사 안 함 ✅
# 컨테이너에서 npm install로 설치한 것만 사용
# 빌드 속도 빠름 + 이미지 크기 작음
node_modules

# 추가로 넣으면 좋을 항목들 
# Git 관련
# 환경 설정 (보안상 제외, 필요하면 docker-compose에서 주입)
# 개발 도구
# 로그 파일
# 테스트 커버리지
# 빌드 결과물 (컨테이너에서 다시 빌드할 거니까)
# OS 파일
# 문서
# Docker 자체